extends layout
                                
block content
  section.game
    .controller.left
    .main
      canvas#field
      canvas#score
    .controller.right
  section.information
    .container
      h1 #{displayName} でゲームに参加中！
  script#main(src="/javascripts/index/bundle.js", data-displayName=`${displayName}`, data-thumbUrl=`${thumbUrl}`, data-ipAddress=`${ipAddress}`)
